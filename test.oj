type test = Test of int * int * int;
let t = (let a = Test (1, 2, 3) in a);
match t with
    | []             -> []
    | Test (x, y, z) -> (
        match y with
            | 1 -> 0
            | 2 -> (let w = z in w + x)
            | 3 -> 69
    )